# .NET
bin/
obj/
*.user
*.suo
.vs/
*.nupkg
*.snupkg
TestResults/

# SvelteKit / Node
node_modules/
.svelte-kit/
build/
dist/
.env
.env.*
!.env.example

# Local TLS certificates (generated by mkcert, never commit)
apps/frontend/.cert/

# Misc
.DS_Store
Thumbs.db

# Local dev secrets — never commit (leaked once, now ignored)
apps/backend/src/Medika.Api/appsettings.Development.json

# Log files — never commit
apps/backend/src/Medika.Api/logs/
*.log

# Claude Code — local overrides contain secrets (MCP tokens, etc.)
.claude/settings.local.json
**/.claude/settings.local.json
.claude/worktrees/

# VSCode — personal editor preferences, not project config
.vscode/

# Playwright MCP run artifacts
.playwright-mcp/
