# Playwright E2E config — committed on purpose (no real secrets here).
# These are the dev-seed credentials created by MongoDbInitializer.SeedAsync,
# usable only against a local dev database. Override any value via the shell
# (e.g. E2E_PASSWORD=... pnpm test:e2e) — process.env wins over this file.

E2E_BASE_URL=https://localhost:5000
E2E_EMAIL=kader.kebir@gmail.com
E2E_PASSWORD=Doctor@123
